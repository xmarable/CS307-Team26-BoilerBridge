"use client"

type GroupSummary = {
    groupID: string;
    groupName: string;
}

export default function GroupPollsPanel({ activeGroup, userId }: { activeGroup: GroupSummary | null, userId: string }) {
    return (
        <div></div>
    )
}