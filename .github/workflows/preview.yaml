name: App Platform Preview

on:
  pull_request:
    branches: [main]

concurrency:
  group: preview-${{ github.event.number }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write
  deployments: write

# Fixes Node.js 20 deprecation warning
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  preview:
    runs-on: ubuntu-latest
    # Define output so the environment can see it after the steps run
    outputs:
      live_url: ${{ fromJson(steps.deploy.outputs.app).live_url }}
    environment:
      name: Preview/pr-${{ github.event.number }}
      # We leave URL out of this block to avoid the JToken error at startup
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install doctl
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Log in to DigitalOcean Container Registry
        uses: docker/login-action@v3
        with:
          registry: registry.digitalocean.com
          username: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}
          password: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push PR image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: registry.digitalocean.com/boilerbridge-registry/boilerbridge:pr-${{ github.event.number }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            MONGODB_URI=${{ secrets.MONGODB_URI }}

      - name: Clean up old images
        run: doctl registry garbage-collection start --include-untagged-manifests --force

      - name: Deploy PR Preview
        id: deploy
        uses: digitalocean/app_action/deploy@v2
        env:
          IMAGE_TAG_boilerbridge: "pr-${{ github.event.number }}"
        with:
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}
          app_spec_location: app.yaml
          deploy_pr_preview: "true"
          print_build_logs: true
          print_deploy_logs: true

      - name: Comment PR
        uses: actions/github-script@v7
        with:
          script: |
            const appOutput = JSON.parse('${{ steps.deploy.outputs.app }}');
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `🚀 BoilerBridge Preview deployed at ${appOutput.live_url}`
            })

  # This small job updates the GitHub Environment UI with the URL once it's known
  set-environment-url:
    needs: preview
    runs-on: ubuntu-latest
    environment:
      name: Preview/pr-${{ github.event.number }}
      url: ${{ needs.preview.outputs.live_url }}
    steps:
      - run: echo "Deployment URL updated in GitHub UI."
